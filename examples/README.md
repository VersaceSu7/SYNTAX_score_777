# Examples Directory

Use this directory for local example inputs and generated outputs.

The directory is ignored by default to avoid accidentally publishing private
study identifiers or derived patient data.
